{
  "name": "wireguard-gui",
  "version": "1.0.0",
  "description": "WireGuard GUI Application",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  },
  "engines": {
    "node": "18.x"
  }
} 